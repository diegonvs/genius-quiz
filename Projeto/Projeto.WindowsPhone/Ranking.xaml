<Page
    x:Class="Projeto.Ranking"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Projeto"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="White">
        <Canvas>           
            <TextBlock Style="{StaticResource fontePrincipal}" HorizontalAlignment="Center" Text="Ranking" />                        
        </Canvas>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ListBox Style="{StaticResource listboxRanking}" x:Name="ListaRanking"  Height="350" Margin="10" Background="White" ></ListBox>
            <Button Content="Voltar" Style="{StaticResource bQuest}" x:Name="Voltar" Click="Voltar_Click" ></Button>
            <Button Content="Limpar Dados" Style="{StaticResource bQuest}" x:Name="Limpar" Click="LimparRanking_Click" />
        </StackPanel>
        </Grid>
</Page>
